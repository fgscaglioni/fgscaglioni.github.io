---
import BaseLayout from "../../layouts/BaseLayout.astro";
import Navbar from "../../components/Navbar.astro";
import { getCollection, render } from "astro:content";

export async function getStaticPaths() {
  const posts = await getCollection("blog");
  return posts.map((post) => ({
    params: { slug: post.id },
    props: { post },
  }));
}

const { post } = Astro.props;
const { Content } = await render(post);
---

<BaseLayout title={`${post.data.title} | Blog Fabrício Scaglioni`}>
  <Navbar slot="header" />

  <article class="pt-8 pb-16">
    <div class="mb-8 border-b border-white/5 pb-6">
      <a
        href="/blog"
        class="inline-flex items-center gap-2 text-xs font-bold uppercase tracking-wider text-slate-500 hover:text-slate-300 transition-colors mb-6"
      >
        <i class="fa-solid fa-arrow-left text-[10px]" /> Voltar ao Journal
      </a>
      <span class="font-mono text-[9px] text-slate-500 uppercase tracking-widest block mb-2">
        {
          post.data.pubDate.toLocaleDateString("pt-BR", {
            year: "numeric",
            month: "long",
            day: "numeric",
          })
        }
      </span>
      <h1 class="text-3xl sm:text-4xl font-display italic text-white leading-tight mb-4">
        {post.data.title}
      </h1>
      <p class="text-slate-400 text-sm italic font-light">
        {post.data.description}
      </p>
    </div>

    <!-- Article Content -->
    <div class="prose prose-invert max-w-none text-slate-300 text-sm leading-relaxed space-y-5
      prose-headings:font-display prose-headings:italic prose-headings:text-white
      prose-h2:text-xl prose-h2:border-b prose-h2:border-white/5 prose-h2:pb-2 prose-h2:mt-8
      prose-p:leading-relaxed
      prose-a:text-white prose-a:underline prose-a:underline-offset-2 prose-a:hover:text-slate-300
      prose-code:text-rose-400 prose-code:bg-white/5 prose-code:px-1.5 prose-code:py-0.5 prose-code:font-mono prose-code:text-xs prose-code:rounded-md
      prose-pre:bg-[#050505]/60 prose-pre:border prose-pre:border-white/5 prose-pre:p-4 prose-pre:rounded-xl prose-pre:font-mono
      prose-strong:text-white prose-strong:font-medium
      prose-ul:list-square prose-ul:pl-5 prose-ul:space-y-2
      prose-li:text-slate-300">
      <Content />
    </div>
  </article>
</BaseLayout>
